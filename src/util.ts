import { attributes } from "./attributes";

export class util {
    //Just a class to store various helper functions

    pointBuy() {
        let list = [8,8,8,8,8,8];
        let points = 27;
        let costs = {9:1, 10:2, 11:3, 12:4, 13:5, 14:7, 15:9};
        while (points > 0) {
            //somehow generate a point buy of attributes
        }

        let attributes: attributes = {};
    }
}