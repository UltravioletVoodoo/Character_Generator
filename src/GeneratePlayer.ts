import { util } from "./Util";
import { Character } from "./Character"
import { pointBuy } from "./PointBuy";
import { sumSkills } from "./Skills";


//Maybe this entire thing is useless. Just do the stuff you need to do elsewhere. Why spend the time writing this

export function generatePlayer(race: Partial<Character>, characterClass: Partial<Character>): Character{
    const attr = //generate attributes
    const attrMods = //generate attribute modifiers
    
    return {

    }   
}